export enum Operators {
  EQUAL = 'eq',
  DIFFERENT = 'ne',
  LESS_THAN = 'it',
  GREATER_THAN = 'gt',
  LESS__EQUAL_THAN = 'le',
  GREATER_EQUAL_THAN = 'ge',
  CONTAINING = 'containing',
  LOWER = 'lower',
  AND = 'and',
  OR = 'or',
}
